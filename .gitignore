# Claude Code local settings
.claude/

# macOS
.DS_Store

# Xcode build products (regenerated by XcodeGen — see README)
build/
DerivedData/
*.xcodeproj/
xcuserdata/

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (not used, but ignored just in case)
Pods/

# Firebase secrets — never commit your real config
GoogleService-Info.plist

# Local scratch downloads of CI screenshot artifacts
ci-artifacts/
